<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <p><label>Nom</label> : {{ contact.lastname }}</p>
        <p><label>Prénom</label> : {{ contact.firstname }}</p>
        <p><label>Email</label> : {{ contact.email }}</p>
        <p><label>Numéro de téléphone</label> : {{ contact.phone }}</p>
        <p><label>Date de naissance</label> : {{ contact.birthday|date("d/m/Y") }}</p>
        <p><label>Univers</label> : </p>
        <ul>
            {% for univers in contact.univers %}
                <li>{{ univers|univers_label|trans({}, 'univers')  }}</li>
            {% endfor %}
        </ul>
        <p><label>Pourquoi souhaitez-vous intégrer le staff ?</label> : {{ contact.whyWishYouJoin }}</p>
        <p><label>Qu'est ce que vous aimeriez idéalement faire ?</label> : {{ contact.whatDoYouLikeToDo }}</p>
        <p><label>Quelles sont vos disponibilités ?</label> : </p>
         <ul>
            {% for availability in contact.availabilities %}
                <li>{{ availability.day|trans({}, 'schedule') }} {{ availability.part|trans({}, 'schedule') }}</li>
            {% endfor %}
        </ul>
    </body>
</html>
